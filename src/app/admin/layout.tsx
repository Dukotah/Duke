export const metadata = { title: 'Pipeline | Copper Bay Tech' }
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen bg-[#F4F4F5]">{children}</div>
}
